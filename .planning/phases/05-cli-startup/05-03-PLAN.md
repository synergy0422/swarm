---
phase: 05-cli-startup
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - swarm/__init__.py
autonomous: true
user_setup: []
gap_closure: true
---

<objective>
Remove RuntimeWarning when running `python -m swarm.cli`.

The issue: `swarm/__init__.py` line 32 eagerly imports the cli module. When running `python -m swarm.cli`, Python has already imported swarm.cli, then __init__.py imports it again, causing "swarm.cli found in sys.modules" warning.

Purpose: Clean import behavior without warnings.
Output: Removed cli import from __init__.py, cli accessible via explicit import only.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/AAA/swarm/.planning/phases/05-cli-startup/05-01-SUMMARY.md
@/home/user/AAA/swarm/.planning/phases/05-cli-startup/05-cli-startup-UAT.md
@/home/user/AAA/swarm/swarm/__init__.py
</context>

<tasks>

<task name="Task 1: Remove cli import from swarm/__init__.py">
  <files>swarm/__init__.py</files>
  <action>
  Remove line 32: `from swarm import cli`

  Remove line 62: `'cli',` from the __all__ list

  This makes the cli module accessible only via explicit import `from swarm import cli` or `from swarm.cli import main`, rather than automatically when importing swarm package.

  The cli module is still fully functional via:
  - `python3 -m swarm.cli` (module execution)
  - `swarm` command after `pip install -e .` (console_scripts entry point)
  </action>
  <verify>
  Run `python3 -W error::RuntimeWarning -m swarm.cli status --cluster-id default 2>&1`
  Expected: No RuntimeWarning, command executes normally
  </verify>
  <done>
  `python3 -m swarm.cli` runs without RuntimeWarning
  </done>
</task>

</tasks>

<verification>
Run the following verification:
```bash
cd /home/user/AAA/swarm
# Test 1: No RuntimeWarning
python3 -W error::RuntimeWarning -m swarm.cli status 2>&1
echo "Exit code: $?"

# Test 2: Verify explicit import still works
python3 -c "from swarm import cli; print('cli module accessible:', hasattr(cli, 'main'))"
```

Expected: No RuntimeWarning errors, cli module still accessible via explicit import.
</verification>

<success_criteria>
- `python3 -m swarm.cli` runs without RuntimeWarning
- `from swarm import cli` still works for explicit imports
- cli entry point via `pip install -e .` still works
</success_criteria>

<output>
After completion, create `.planning/phases/05-cli-startup/05-03-SUMMARY.md`
</output>
