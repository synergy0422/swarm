---
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
---

## 验证 README.md 快速启动小节

### 目标

验证 README.md 的"5 窗格布局"章节中已添加"快速启动"小节，包含正确的一行命令示例。

### 验证标准 (must_haves)

- [ ] README.md 包含 `### 快速启动` 小节
- [ ] 小节位于"5 窗格布局"章节内，参数说明表格之后
- [ ] 包含命令：`LLM_BASE_URL="http://127.0.0.1:15721" SWARM_WORKDIR="$PWD" CODEX_CMD="codex --yolo" ./scripts/swarm_layout_5.sh --attach`
- [ ] 包含说明文字："在任意目录执行以下命令，即可使用本地代理启动 5 窗格布局，并让 codex 默认使用 --yolo"

### 验证步骤

```bash
# 1. 检查快速启动小节存在
grep -q "### 快速启动" README.md

# 2. 检查小节位于正确的章节内（在"5 窗格布局"和"诊断快照"之间）
grep -n "### 快速启动" README.md > /tmp/quick_start_line.txt
grep -n "## 诊断快照" README.md > /tmp/snapshot_header_line.txt
grep -n "## 5 窗格布局" README.md > /tmp/layout_header_line.txt

# 3. 检查命令格式正确
grep -q "LLM_BASE_URL.*127.0.0.1.15721" README.md
grep -q "SWARM_WORKDIR=\"\$PWD\"" README.md
grep -q "CODEX_CMD=\"codex --yolo\"" README.md
grep -q "./scripts/swarm_layout_5.sh --attach" README.md

# 4. 检查说明文字
grep -q "在任意目录执行以下命令" README.md
```

### 通过条件

所有验证步骤返回成功（退出码 0）。

---
